# Microstepping Test Data

- v1/ contains the migrated existing measurements, scripts, and rendered
  assets.
- v2/ is the clean workspace for the next revision.

The scripts inside each revision resolve data and outputs relative to that
revision directory.
